device: cpu

llm:
  model: Qwen/Qwen2.5-3B-Instruct
  max_tokens: 1024

chunking:
  size: 1000
  overlap: 100

extraction:
  min_confidence: 0.5   # steps below this are flagged invalid
  refine_invalid: true  # run a second LLM pass on invalid steps
